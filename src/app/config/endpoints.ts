import {environment} from '../../environments/environment';

const host = environment.host;
export const API_PACKAGE = `${host}/package`;
export const API_PLACE = `${host}/place`;
export const API_TRANSPORT = `${host}/transport`;
export const API_USER = `${host}/user`;
export const API_REGISTRY = `${host}/registry`;
export const API_SHIPPING = `${host}/shipping`;
